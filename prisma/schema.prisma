generator client {
  provider = "prisma-client-js"
  previewFeatures = ["postgresqlExtensions"]
}

datasource db {
  provider = "postgresql"

  extensions = [postgis]
}

model Rota {
  id Int @id @default(autoincrement())
  nome String 
  trajeto Unsupported("geometry(LineString, 4326)")
}
